pipeline {
    agent any
    
    stages {
        stage("Build & making tar file") {
            steps {
                sh "tar -cvf pythontar.${img_tag}.tar demo-backend2"
            }
        }
    }
}
